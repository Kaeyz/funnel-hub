export const queryFormFields = "";

export enum QuestionSortKeys {
  Name = "name",
  Key = "key",
  ORDER = "order",
}

export enum QuestionStatus {
  DRAFT = "draft",
  ACTIVE = "active",
  ARCHIVED = "archived",
}

export enum QuestionType {
  TEXT = "text",
  NUMBER = "number",
  FILE = "file",
}
